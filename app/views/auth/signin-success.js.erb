$('.auth-error').text('')
$('#sign-in').bPopup().close()

var scope = $('body').scope()
scope.$apply(function(){
  scope.signedIn = true
  scope.user = <%= current_user.to_json(include: :cards).html_safe %>
})