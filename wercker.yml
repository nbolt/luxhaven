box: wercker/ruby

services:
  - wercker/postgresql

build:
  steps:
    - bundle-install
    - rails-database-yml

deploy:
  steps:
    - heroku-deploy
    - script:
      name: Migrate database
      code: heroku run rake db:migrate --app $TARGET_NAME